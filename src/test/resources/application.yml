spring:
  datasource:
    url: jdbc:h2:mem:library
  main:
    allow-circular-references: true

  sql:
    init:
      mode: always
      data-locations: data.sql

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

    show-sql: true

logging:
  level:
    ROOT: ERROR